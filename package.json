{
  "name": "iTunesApp",
  "version": "1.0.0",
  "description": "iTunes api proxy",
  "main": "app.js",
  "scripts": {
    "build": "(npm install) && (npm install --prefix ./client)",
    "start": "(node ./app.js &) && (npm start --prefix ./client)",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Vishnubabu/iTunesApp.git"
  },
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "express-validator": "^4.3.0",
    "node-cache": "^4.1.1",
    "request": "^2.83.0"
  }
}
